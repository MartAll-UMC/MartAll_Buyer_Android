<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:usesCleartextTraffic="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.App.Starting"
        tools:targetApi="31">
        <activity
            android:name=".view.home.NewMerchActivity"
            android:theme="@style/Theme.MartAll" />
        <activity
            android:name=".view.mypage.customerservice.PrivacyPolicyActivity"
            android:theme="@style/Theme.MartAll" />
        <activity
            android:name=".view.store.ProductDetailActivity"
            android:theme="@style/Theme.MartAll" />
        <activity
            android:name=".view.mypage.customerservice.ServiceTermsActivity"
            android:theme="@style/Theme.MartAll" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:saveEnabled="false">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".view.home.notification.NotificationActivity"
            android:exported="true"
            android:theme="@style/Theme.MaterialComponents.DayNight.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
            </intent-filter>
        </activity>
        <activity
            android:name=".view.store.StoreActivity"
            android:theme="@style/Theme.MartAll" />
        <activity android:name=".view.mypage.customerservice.CustomerServiceActivity" />
        <activity
            android:name=".view.likelist.DibsActivity"
            android:theme="@style/Theme.MartAll" />

        <activity android:name=".view.store.MartDetailInfoActivity"
            android:theme="@style/Theme.MartAll" />
        <activity
            android:name=".view.search.SearchActivity"
            android:theme="@style/Theme.MartAll" />
    </application>

</manifest>